name: 📊 Análise PRF 2024 | 🧬 Estatística e Probabilidade Unidade I

# Define quais eventos disparam a execução automática.
on:
  push:
    branches:
      -  main  # Em novos commits para a branch main
  pull_request:
    types: [ opened, synchronize, reopened ] # Em pull requests para a branch main

jobs:
  build:
    # Maquina virtual onde a ação será executada
    runs-on: ubuntu-latest
    steps:
      # 1. Faz o checkout do código para a máquina virtual
      - name: "📂 Sincronizando Repositório"
        uses: actions/checkout@v4

      # 2. Configura o ambiente R 4.5.2
      - name: "⚙️ Configurando Engine R (4.5.2)"
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.5.2' # Minha versão específica do R

      # 3. Gerenciamento e instalação de pacotes necessários
      - name: "📦 Instalando Bibliotecas (dplyr, ggplot2, scales, tidyr, testthat)"
        run: |
          install.packages(c("dplyr", "ggplot2", "scales", "tidyr", "testthat"), repos = "https://cloud.r-project.org/")
        shell: Rscript {0}

      # 4. Executa o script principal de análise (inclui análises + testes + gráficos)
      - name: "🚀 Executando Análise Estatística"
        run: |
          source("scripts/main.R")
        shell: Rscript {0}

      # 5. Salva os gráficos gerados como artefatos de build
      - name: "🖼️ Salvando Resultados (Gráficos)"
        uses: actions/upload-artifact@v4
        with:
          name: "Relatorio_Visual_PRF"
          path: graficos/*.png
